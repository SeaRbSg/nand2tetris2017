/**
 * Compares 2 16-bit numbers
 * if a==b, true
 * else, false
 */

CHIP Comp16 {

    IN a[16], b[16];

    OUT out;

    PARTS:
    Eq16(a=a,b=b,out=eq);
    Not16(in=eq,out=invertedeq);
    Or16Way(in=invertedeq,out=noteq);
    Not(in=noteq,out=out);
}
