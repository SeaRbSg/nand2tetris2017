/**
 * Multiplexor:
 * out = a if sel == 0
 *       b otherwise
 */

CHIP Mux {
    IN a, b, sel;
    OUT out;

    PARTS:
    Not(in=sel, out=nsel);
    And(a=sel, b=b, out=selb);
    And(a=a, b=nsel, out=sela);
    Or(a=sela, b=selb, out=out);
}
