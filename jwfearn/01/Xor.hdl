/**
 * Exclusive-or gate:
 * out = not (a == b)
 */

CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    Nand(a=a, b=a, out=na);
    Nand(a=b, b=b, out=nb);
    Nand(a=a, b=nb, out=anb);
    Nand(a=na, b=b, out=nab);
    Nand(a=anb, b=nab, out=out);
}
